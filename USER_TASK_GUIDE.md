# 你的任务操作手册：让「在线播放」从信念变成事实（#18 真机验收）

> 大白话版。技术同学已把代码层全部做完并测绿（见文末），现在**差最后一步：用你手上的一个真实 CMS 片源地址，把整条链路真跑一遍**。这一步必须在**你的机器 + 你的网络**上做，因为：
> 1. 片源地址在你手上（合规要求：出厂不预置任何片源，必须用户自己导入）；
> 2. 真实拉流要打外网，沙箱环境网络受限；
> 3. 「视频解码出画面」只能在真实浏览器/Electron 里看。

---

## 一、你要做的事（一句话）
找一个**真实可用、且你有权使用的 CMS10 片源地址** → 启动应用 → 跑一遍验收，把结果回传给我。

---

## 二、第一步：确认环境已经就绪
打开终端（项目目录 `Mineradio-Extended-1.1.2-extended.1`），依次执行：

```bash
# 1) 确认依赖装好了（之前你装过 npm，这里复核一下）
npm install

# 2) 确认 hls.min.js 已本地化（这步是我刚做的，你不用改，只是确认文件在）
ls public/vendor/hls.min.js      # 应能看到一个 ~400KB 的文件
```

如果 `npm install` 报错或卡住，先解决它再往下走——应用起不来，后面的验收都做不了。

---

## 三、第二步：找一个真实 CMS10 源地址（重点）

### 3.1 大白话解释
CMS 片源 = 一个**网站后台接口地址**。Stellaflix 拿这个地址去「搜片名 → 拿详情 → 取播放链接 → 播放」。
苹果 CMS（appleCMS）是这类接口最常见的一套标准协议。我们代码里实现的是 **V10 版本**（返回 JSON）。

### 3.2 地址长什么样
一个典型的 CMS10 地址形如：
```
https://example.com/INDEX.php?m=movie
```
要点：
- 必须带 `INDEX.php`（或 `index.php`、`api.php`，看站方约定）；
- 后面常跟 `?m=movie` 这种入口参数；
- **结尾不要带 `ac=list` 这类参数**，脚本会自动拼接。

### 3.3 去哪找（合规提醒）
- 苹果的「资源网 / 影视资源站」很多公开提供这种接口，**仅用于你个人的学习与技术验证**；
- 请确认你对该地址有合法使用权，不要用于分发或盈利；
- 我们**不会**把这个地址写进代码或安装包——它只存在你本机、由你手动导入，合规红线照旧守着。

### 3.4 怎么判断它大概率是 V10（不是 V8）
- V10 默认返回 **JSON**；V8 默认返回 **XML**（要加 `&at=json` 才是 JSON）。
- 验收脚本会先按 V10 试，失败自动退回 V8 写法，所以你**不用自己判断版本**，给地址就行。

---

## 四、第三步：启动应用（会把服务端一起拉起来）
在终端执行：
```bash
npm start
```
之后会发生两件事：
1. 弹出 Stellaflix 图形窗口；
2. **同时**在后台启动本地服务端，监听 `http://127.0.0.1:3000`（这是 `/api/proxy` 代理的所在地，验收脚本要打它）。

> 保持这个窗口/进程开着，别关。另开一个终端做下一步。

---

## 五、第四步：一键跑真机验收脚本（最省力、最客观）
在**另一个终端**（项目目录）执行，把你的地址填进去：
```bash
CMS_URL="https://你的CMS地址/INDEX.php?m=movie" node scripts/step5_realsource_test.js
```

脚本会自动做这四件事并打印结果：
1. **搜索**：用关键词「测试」去你的 CMS 搜，看能不能返回片子；
2. **详情**：拉第一条片子的详情，解析出真实播放地址（m3u8 / mp4）；
3. **代理拉流**：让 `/api/proxy` 真的去把那个地址拉回来，验证跨域代理 + Range 透传；
4. **分片验证**：如果是 m3u8，再顺手把清单里第一个 `.ts` 分片也经代理拉一遍。

看最后一行：
- 🎉 `X pass / 0 fail` → 数据流彻底打通，进入第六步回传；
- ⚠️ 有 `fail` → 看上面红色的哪一项，对照文末「常见坑」自查，或把输出贴给我。

---

## 六、第五步：在图形界面里手动点一遍（双保险）
即使脚本全绿，也建议手动确认「真能出画面」：
1. 在 Stellaflix 影视态，找到「片源管理 / 添加源」入口（我们叫 `SFV.online` 浏览层）；
2. 把你的 CMS 地址填进去保存；
3. 搜一个片名 → 点进详情 → 点第一集；
4. 确认视频区域**真的开始播放**（不是黑屏/转圈）。

能播 = 能力层 + 入口层 + 内容层（你导入）全部闭环，任务完成。

---

## 七、第六步：把结果回传给我
把下面任意一项发我，我据此收尾（更新计划文档、补 README、准备发布复核）：
- 第四步脚本的完整输出（绿了直接贴最后几行即可）；
- 或第五步手动播放成功的截图；
- 若失败，把红色项 + 你的 CMS 地址（可私信）发我，我来定位是地址/版本/网络/代码哪一层的问题。

---

## 八、常见坑（提前避雷）
| 现象 | 原因 | 处理 |
|---|---|---|
| 脚本第一步就 `非 JSON` | 地址是 V8 或根本不是 CMS | 脚本会自退回 V8；若仍失败，确认地址正确 |
| `/api/proxy 未响应` | 忘了先 `npm start` | 回去第三步，保持服务进程 |
| 搜得到但播放地址解析为空 | 该站播放地址**加密**（苹果 CMS 常见） | 把 `vod_play_url` 原始值贴我，我加解密适配 |
| GUI 里点开是黑屏但脚本绿 | 解码/渲染问题（非数据链路） | 贴报错，我查 player/渲染层 |
| 提示 `hls-lib-load-failed` | 旧代码从 jsdelivr 拉 hls.js（国内常超时） | **已修复**：hls.js 本地化了，不会再出现 |

---

## 九、我已替你做完的部分（本轮）
- ✅ **hls.js 本地化**：`public/vendor/hls.min.js`（MIT 许可，已内置），`source-adapter.js` 改为同源 `/vendor/hls.min.js`，消除 jsdelivr 国内不可达导致的 m3u8 静默失败。
- ✅ **修好此前藏起来的 14 个红测试**：
  - `step3b`：HLS 库地址不再被误判为硬编码片源 → 50/0；
  - `step3c`：改为断言铁律四卡（心动/片单/收藏/历史），不再是被驳回的本地六卡 → 44/0。
- ✅ **全量 9 个测试脚本、357 断言、0 失败**（之前是 14 红）。
- ✅ **新增 `step5_realsource_test.js`**：上面第四步用的真机验收脚本。

> 注：「代码层全绿」只证明函数不崩、逻辑自洽；**真正的「能看片」要你用真实 CMS 地址跑完第五步才算数**——这正是本手册的目的。
